/**
 * Thin route definitions.
 * Each route imports and renders a feature page from src/features/{FeatureName}/pages/.
 *
 * Example:
 *   { path: '/clients', element: <ClientsPage /> }
 */

export type AppRoutes = never
